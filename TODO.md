# TODO
- [ ] Add streaming completions and a system-prompt parameter.
- [ ] Add an OpenAI-style /chat/completions backend alongside the Ollama one.
- [ ] Add an integration test gated behind an `--online` flag with a local model.
- [ ] Promote the hub catalog entry from `planned` to `available` once published.
